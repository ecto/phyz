# phyz Benchmarks

phyz's README makes performance claims — "GPU-accelerated", "thousands of
parallel environments", "analytical derivatives for free". This document is the
evidence for and against them.

It is written to be useful to someone deciding whether to use phyz, which means
it reports where phyz loses as plainly as where it wins. There are losses here.
One of them contradicts a README claim.

Generated by [`crates/phyz-bench`](crates/phyz-bench/). Reproduce with:

```bash
make bench
```

---

## Summary

| Claim | Verdict |
|---|---|
| Fast single-sim rigid body dynamics | **Holds.** 4.1 M steps/s on a pendulum, 229 k on a 14-DOF ant; marginally faster than Rapier (1.0–1.1×) on reduced-coordinate chains at matched precision. |
| GPU-accelerated batch simulation | **Holds above batch ~128.** 36× a CPU thread at batch 4096. Below batch 128 the GPU path is *slower* than one CPU core — at batch 1 it is 100× slower. |
| "Thousands of parallel environments" | **Holds.** 16 384 environments run at 7.8 M env-steps/s. Throughput plateaus after ~4096. |
| Contact / collision performance | **Loses badly.** Rapier is ~26× faster on an 8-box stack. |
| "Analytical derivatives for free" | **Not free; not the same class as finite differences either.** A gradient rollout costs 6–9× a forward rollout per DOF, and that ratio is flat in parameter count — 160 parameters at one DOF costs 9.1×, against 6.5× for 10 parameters. So it beats finite differences by a margin that grows with parameter count (3.0× at 10 parameters, 18.4× at 80, measured) and loses to textbook reverse mode's small constant. "Free" is wrong; "the same scaling as finite differences", which this table said until the `adjoint scaling` suite tested it, was also wrong. |
| Numerical quality | **Holds.** Energy error falls as O(dt) exactly as a symplectic integrator should, on both a simple and a chaotic scene. |

Detail, settings, and caveats below. **Read the caveats before quoting a
number** — particularly for the contact comparison, where the two engines are
not running the same algorithm and cannot be made to.

---

## Methodology

### Measurement protocol

Identical for every engine, which is the only way a cross-library number means
anything:

| | |
|---|---|
| Warmup | 2 repetitions, discarded |
| Timed repetitions | 7 |
| Work per repetition | 20 000 steps (single-sim and comparison suites) |
| Statistic | **Median** of the 7, chosen for robustness to a scheduler hiccup |
| Also reported | min, max, and spread `(max−min)/median` |
| Build | `--release`, no `target-cpu=native` unless recorded in the run metadata |

Every repetition resets the scene to its initial state inside the timed
closure, so repetition 7 simulates the same trajectory as repetition 1. The
Rapier world is rebuilt per repetition, because Rapier puts settled islands to
sleep and reusing a world would flatter it.

### Honesty mechanisms

Enforced by the harness, not by good intentions:

- **Spread is published, and noise is flagged.** Any row over 25% spread is
  marked ⚠ and triggers a per-suite warning. Do not quote a ⚠ row.
- **Instability cannot masquerade as speed.** Every timed scene is checked for
  a finite final state, because a diverged simulation does *less* work per
  step. Every row carries a `stable` metric.
- **Debug builds are labelled invalid** in the run metadata and warned about at
  startup.
- **Skips are recorded with reasons**, never silently omitted.
- **The gradient suite cross-checks itself** against finite differences
  (`adjoint_vs_fd_max_rel_err`). A fast gradient that disagrees with FD is not
  a result.

### Physics settings

Single source of truth:
[`crates/phyz-bench/src/settings.rs`](crates/phyz-bench/src/settings.rs). Every
JSON record embeds the full settings block it was measured under.

| Setting | Articulated scenes | Contact scenes |
|---|---|---|
| Timestep | 1 ms | 2 ms |
| Gravity | `(0, 0, −9.81)` m/s² | same |
| Solver iterations | 4 (inert for phyz — it has no velocity solver) | 4 |
| Contact stiffness | — | 1×10⁵ N/m |
| Contact damping | — | 1×10³ N·s/m |
| Friction | — | 0.5 |
| Restitution | — | 0.0 |

### Scenes

| Scene | Definition |
|---|---|
| `pendulum` | 1 revolute link, 1 m, 1 kg, thin-rod inertia, hinged about **+Y**, released at 1.0 rad |
| `double_pendulum` | 2 such links, released at (1.0, 0.5) rad — chaotic, so it tests the integrator rather than a closed form |
| `ant` | `models/ant.xml` via the MJCF loader: free-floating torso + 8 hinges, 14 DOF |
| `box_stack_8` | 8 free 10 cm cubes, 1 kg each, 1 cm initial gaps, on a ground plane |

The hinge axis is +Y, not phyz's default +Z. With a rod hanging along −Z under
−Z gravity, a Z hinge puts the centre of mass *on* the rotation axis: the
gravitational torque is identically zero and the "pendulum" never moves. (The
pre-existing benchmark in `crates/phyz-diff/benches/jacobians.rs` has this
problem and is benchmarking a stationary body.)

### What phyz's "step" means here

phyz ships no `Simulator` facade, so the benchmark loop is assembled from the
public pieces a user would reach for:

```
forward_kinematics  →  (ground + pairwise contact)  →  aba_with_external_forces  →  semi-implicit Euler
```

Contact scenes run **both** contact paths — the ground-plane query and
sweep-and-prune + GJK/EPA between boxes — so the measured cost includes
narrow-phase collision, as Rapier's does.

---

## Cross-library comparison

### Rapier

`rapier3d-f64` 0.34, in-process, no FFI in the measurement.

**Built as f64 deliberately**, to match phyz's precision. Stock `rapier3d` is
f32 and would likely be faster than the numbers here. That choice is documented
rather than exploited in either direction.

What is held equal: timestep, gravity, masses, inertia tensors, link lengths,
initial angles, friction coefficient, restitution, solver-iteration request.

**What cannot be held equal:**

- **The pendulums are close to like-for-like.** Rapier's multibody joint set is
  reduced-coordinate, like phyz's ABA. This is a fair fight.
- **The box stack is not a controlled experiment.** phyz uses a compliant
  penalty force (stiffness/damping); Rapier uses an impulse solver with
  penetration slop. There is no setting that makes these the same algorithm.
  Only friction and restitution translate. Read that row as "cost of each
  engine's own contact story on the same scene at the same timestep".
- **The ant has no Rapier row.** It is loaded from MJCF, which Rapier does not
  parse in-tree. Hand-building it would risk an unequal model, so no comparison
  is published rather than a bad one.
- **`solver_iterations = 4` equalises what we asked for, not the arithmetic
  performed.** phyz has no velocity solver at all.

### MuJoCo / MJX

A separate, optional Python harness:
[`crates/phyz-bench/python/`](crates/phyz-bench/python/). Deliberately **not** a
CI dependency and not part of `make bench` — MuJoCo needs a Python environment
and MJX needs JAX plus a GPU to be worth running, and making either mandatory
would put the benchmark suite out of reach for most people.

It emits the same JSON schema, so results merge into one document. See that
directory's README for the full caveat list; the load-bearing ones are that
MuJoCo's soft-constraint contact is a third distinct algorithm, that Python FFI
overhead is material for the pendulum scene specifically, and that MJX is f32.

---

## Results

<!-- BEGIN GENERATED RESULTS -->
<!-- Regenerate with `make bench`, then paste bench-results.md between these markers. -->

### Run metadata

- **Date:** 2026-07-25T01:48:43Z
- **Hardware:** Apple M4 Max (16 logical cores, 48 GiB RAM)
- **OS:** Darwin 25.5.0 (macos aarch64)
- **Toolchain:** rustc 1.95.0 (59807616e 2026-04-14) — profile `release`, no `RUSTFLAGS`
- **phyz:** 0.1.0 (f64 throughout)
- **rapier3d-f64:** 0.34.0 (f64 build, chosen to match phyz precision)
- **Commit:** `0c6147f9ecd9`

Every row below **except the GPU table** came from this single run. Spread is
`(max−min)/median` over 7 repetitions; no row exceeded the 25% noise threshold.

The GPU table is separately sourced and labelled as indicative — see that
section. The recording machine was shared with other work during this session,
and runs were repeated until one came back with zero ⚠ rows; a clean full run
including the GPU sweep is the outstanding item. That is a limitation of the
recording session, not of the harness, and it is stated here rather than
papered over.

### Single-sim throughput

Steps per second, one simulation, one CPU thread. `realtime` is simulated
seconds per wall-clock second.

| Scene | DOF | dt (s) | Throughput | µs/step | Spread | realtime |
|---|---|---|---|---|---|---|
| `pendulum` | 1 | 0.001 | **4.14 M/s** | 0.241 | 4% | 4142× |
| `double_pendulum` | 2 | 0.001 | **2.40 M/s** | 0.416 | 1% | 2402× |
| `ant` | 14 | 0.001 | **229 k/s** | 4.365 | 3% | 229× |
| `box_stack_8` | 48 | 0.002 | **43.6 k/s** | 22.93 | 21% | 87× |

### Agreement with MuJoCo

Speed comparisons say how fast an engine is. They say nothing about whether it
computes the right thing. `make agreement` asks the other question: given the
same model, the same initial state and the same integrator, do phyz and MuJoCo
trace the same trajectory?

Both engines are stripped to gravity-driven articulated dynamics on a kinematic
tree first — damping, springs, dry friction and joint limits zeroed, contact and
constraints disabled, semi-implicit Euler on both sides — because each of those
is modelled differently by the two and leaving any on would measure the
difference between two approximations of a *constraint* rather than two
computations of the *dynamics*. Armature is kept: it is a constant on the
mass-matrix diagonal, implemented the same way by both, and on light distal
links it is what keeps the mass matrix conditioned.

MuJoCo 3.11.0, 10 000 steps at dt = 1 ms, tolerance 1×10⁻⁶. **The `step 1`
column is the one that matters** — one step from an identical state, with no
room for accumulation, so it isolates whether the two engines compute the same
accelerations:

| model | nq | step 1 | 0.1 s | 1 s | max over 10 s |
|---|---|---|---|---|---|
| `chain_1` | 1 | 2.5×10⁻¹⁵ | 1.4×10⁻¹¹ | 1.1×10⁻¹⁰ | 4.5×10⁻⁹ |
| `chain_2` | 2 | 1.3×10⁻¹⁴ | 5.3×10⁻¹¹ | 6.9×10⁻¹⁰ | 1.0×10⁻⁸ |
| `chain_4` | 4 | 2.1×10⁻¹⁴ | 8.8×10⁻¹¹ | 1.2×10⁻⁹ | 2.7×10⁻⁸ |
| `chain_8` | 8 | 4.1×10⁻¹⁴ | 1.3×10⁻¹⁰ | 3.3×10⁻⁹ | 4.3×10⁻⁸ |
| `chain_x_axis` | 3 | 1.9×10⁻¹⁴ | 9.1×10⁻¹¹ | 1.0×10⁻⁹ | 1.7×10⁻⁸ |
| `tree` (branching) | 4 | 1.8×10⁻¹⁵ | 9.8×10⁻¹² | 2.0×10⁻¹⁰ | 1.8×10⁻⁹ |
| `ant` (free base) | 15 | **0** | 7.9×10⁻¹² | 1.8×10⁻⁶ | 3.1×10⁻¹ |
| `half_cheetah` (free base) | 13 | **0** | 5.4×10⁻¹³ | 5.7×10⁻⁸ | 1.3×10⁻³ |
| `humanoid` (free base) | 24 | 6.5×10⁻¹⁵ | 4.2×10⁻¹⁰ | 1.4×10⁻⁴ | 1.5 |
| `shadow_hand` | 24 | 5.8×10⁻¹⁰ | 9.6×10⁻⁸ | 5.0×10⁻⁹ | 1.2×10⁻⁷ |
| `slide_chain`, `simple_arm` | 3, 2 | 0 | 0 | ≤2×10⁻¹⁷ | ≤1.8×10⁻¹⁵ |

**phyz's Featherstone ABA computes the same accelerations as MuJoCo to f64
epsilon** — on serial chains, a branching tree, off-diagonal inertia tensors,
hinge and slide joints, a 24-DOF hand, and all three free-floating models,
where the first-step difference is exactly zero on two of them. Free bases,
which an earlier revision of this harness could not compare at all, are
included: MuJoCo packs a free joint as position-then-quaternion and phyz as
exponential-coordinates-then-position, and `phyz-traj --layout mujoco` converts
between them.

**What the later columns are, and are not.** They are accumulation, not
disagreement. The seed is machine epsilon at step 1 and grows roughly as `t⁴`
on the free-base models, reaching order 1 rad by 10 s on the humanoid. That is
not chaos amplifying round-off — a broadband 1-ulp perturbation of the same
initial state stays at 8×10⁻¹⁵ over the same window, so the model is not in a
sensitive regime here — it is two numerically distinct but algebraically
equivalent factorizations (ABA against MuJoCo's LDL) integrating for ten
thousand steps from an epsilon-sized difference. **Quote the `step 1` column
for correctness and the later ones for how long a shared trajectory survives.**

`shadow_hand` is the one model whose first step is not at epsilon, at
5.8×10⁻¹⁰. Its link inertias span five orders of magnitude and its mass matrix
is dominated by the armature regularisation, so the two factorizations differ
at the conditioning level rather than at epsilon. It is also the model that
goes non-finite in *both* engines within 10 ms if armature is stripped — which
is why it isn't.

Contact is still not covered, for the same reason the Rapier box-stack row
carries its caveat: the two contact models are different algorithms, and
comparing their trajectories would compare modelling choices rather than
implementations.

Measured on a different host from the generated block above. Reproduce with
`make agreement` (needs `pip install mujoco`).

### Numerical quality — energy drift

Conservative scenes, no contact, no damping. Drift is relative to initial total
mechanical energy. `peak` is the worst excursion along the rollout; `net` is the
endpoint.

**5 s horizon — integrator error, falls as O(dt):**

| Scene | dt (s) | net drift | peak error | wall s / sim s |
|---|---|---|---|---|
| `pendulum` | 4×10⁻³ | 6.2×10⁻³ | 6.2×10⁻³ | 0.0001 |
| `pendulum` | 2×10⁻³ | 3.1×10⁻³ | 3.1×10⁻³ | 0.0001 |
| `pendulum` | 1×10⁻³ | 1.5×10⁻³ | 1.5×10⁻³ | 0.0002 |
| `pendulum` | 2.5×10⁻⁴ | 3.9×10⁻⁴ | 3.9×10⁻⁴ | 0.0010 |
| `double_pendulum` | 4×10⁻³ | 4.6×10⁻² | 5.1×10⁻² | 0.0001 |
| `double_pendulum` | 2×10⁻³ | 2.3×10⁻² | 2.6×10⁻² | 0.0002 |
| `double_pendulum` | 1×10⁻³ | 1.2×10⁻² | 1.3×10⁻² | 0.0004 |
| `double_pendulum` | 2.5×10⁻⁴ | 3.0×10⁻³ | 3.3×10⁻³ | 0.0017 |

**100 s horizon — the simple pendulum still converges; the chaotic one stops
responding to `dt` entirely:**

| Scene | dt (s) | net drift | peak error | wall s / sim s |
|---|---|---|---|---|
| `pendulum` | 4×10⁻³ | 5.0×10⁻⁴ | 6.2×10⁻³ | 0.0001 |
| `pendulum` | 2×10⁻³ | 2.0×10⁻⁴ | 3.1×10⁻³ | 0.0001 |
| `pendulum` | 1×10⁻³ | 1.0×10⁻⁴ | 1.5×10⁻³ | 0.0002 |
| `pendulum` | 2.5×10⁻⁴ | <1×10⁻⁴ | 3.9×10⁻⁴ | 0.0010 |
| `double_pendulum` | 4×10⁻³ | +0.370 | 0.380 | 0.0001 |
| `double_pendulum` | 2×10⁻³ | −0.092 | 0.153 | 0.0002 |
| `double_pendulum` | 1×10⁻³ | +0.118 | 0.119 | 0.0004 |
| `double_pendulum` | 2.5×10⁻⁴ | −0.144 | 0.144 | 0.0017 |

The double pendulum's 100 s error is not monotone in `dt` and does not shrink.
That is chaos, not an integrator defect — see the 5 s table, where the same
integrator on the same scene converges cleanly. Discussion below.

### Gradient throughput

500-step rollout, `J = ½‖q_T‖² + ½‖v_T‖²`, gradient w.r.t. every body's ten
inertia parameters. All three quantities measured; nothing extrapolated.

| Scene | params | forward | adjoint | central FD | adjoint vs FD | FD agreement |
|---|---|---|---|---|---|---|
| `pendulum` | 10 | 0.138 ms | **18.2× fwd** | 20.3× fwd | **1.12× faster** | exact (0.0 rel) |
| `double_pendulum` | 20 | 0.227 ms | **45.0× fwd** | 39.7× fwd | **0.88× — slower** | exact (0.0 rel) |

Doubling the parameter count roughly doubles the adjoint's cost. It should not,
and that is the finding. See discussion below.

### Batched GPU throughput

> **Skipped in this run** — the binary was rebuilt without `--features gpu`, and
> the harness reported the skip rather than silently omitting the suite.
> Re-run with `make bench-gpu` on an idle machine.

Figures from an earlier run on the same hardware, kept here as **indicative
only** because they are not from the run above:

| Batch | Env-steps/s | vs 1 CPU thread |
|---|---|---|
| 1 | 2.0 k | 0.009× |
| 16 | 31 k | 0.14× |
| 128 | 255 k | 1.17× |
| 1 024 | 2.04 M | 9.4× |
| 4 096 | 6.96 M | 32× |
| 16 384 | 7.78 M | 36× |

The shape of that curve — loses below ~batch 128, plateaus after ~4096 — is
consistent across every run taken, but do not quote the individual numbers
until they are reproduced in a clean full run.

### Cross-library comparison (phyz vs Rapier)

`phyz_vs_rapier` > 1 means phyz is faster.

| Scene | DOF | dt (s) | phyz | rapier3d-f64 | phyz vs Rapier |
|---|---|---|---|---|---|
| `pendulum` | 1 | 0.001 | 3.73 M/s | 3.44 M/s | **1.08×** |
| `double_pendulum` | 2 | 0.001 | 2.32 M/s | 2.29 M/s | **1.02×** |
| `ant` | 14 | 0.001 | 231 k/s | — | no equivalent model¹ |
| `box_stack_8` | 48 | 0.002 | 44.4 k/s | 1.17 M/s | **0.038× — 26× slower** |

¹ The ant is loaded from MJCF, which Rapier does not parse in-tree.
Hand-building it risks an unequal model, so no comparison is published rather
than a bad one.

The pendulum rows are close to like-for-like (both engines reduced-coordinate).
**The box-stack row is not a controlled experiment** — penalty contact versus an
impulse solver, with only friction and restitution matched. See the caveats in
the methodology section above.

<!-- END GENERATED RESULTS -->

---

## Interpretation

### Where phyz wins

**Reduced-coordinate articulated dynamics.** phyz's ABA edges out Rapier's
multibody solver on the pendulum scenes at matched precision — 1.08× and 1.02×.
That is a narrow win, and small enough that on another machine it could go the
other way; the honest reading is "competitive", not "faster". For
robotics-shaped problems — a kinematic tree, few or no contacts — this is
phyz's strong suit, and 229 k steps/s on a 14-DOF ant is a genuinely useful
number: 229× real time at a 1 ms timestep.

**Batched GPU rollouts, at scale.** Above batch ~128 the GPU path pulls away,
reaching ~36× a CPU thread at batch 4096 and 7.8 M environment-steps/s at
16 384 environments. "Thousands of parallel environments" is substantiated.

### Where phyz loses

**Contact, by a lot.** Rapier is ~26× faster on an 8-box stack. Some of that is
algorithmic (an impulse solver on a settled stack does very little work; a
penalty model integrates the same forces every step regardless), and some is
that phyz's broad/narrow phase is not optimised. Either way, if your workload
is contact-dominated, the numbers say use Rapier.

**Small-batch GPU.** At batch 1 the GPU path is ~100× *slower* than one CPU
thread, and it does not break even until batch ~128. The crossover is the
number that matters, and it should be in the README next to the word
"GPU-accelerated". Throughput also plateaus between 4096 and 16 384, so the
last 4× in batch size buys ~1.1× in throughput.

**Gradients — expensive, but not for the reason this document used to give.**

An earlier revision of this section read the gradient suite's two rows — 18×
the forward pass at 10 parameters, 45× at 20 — and concluded that the adjoint
costs a lane per parameter, putting it in finite differences' asymptotic class.
That conclusion was wrong, and it was wrong because two scenes cannot support
it. The pendulum has one body, one DOF and ten parameters; the double pendulum
has two of each. Parameter count and DOF count move together, so those rows are
equally consistent with either explanation.

The `adjoint scaling` suite separates them. `weld_chain_N` hangs `N` rigidly
welded bodies off a single revolute joint: every welded body carries a full
ten-parameter spatial inertia and is dynamically live, but a `Fixed` joint adds
no DOF, so the parameter count grows 16× while `nv` stays at 1. `dof_chain_N`
is the control, growing both together as the gradient suite's scenes do.

The tables below come from `phyz-bench --suite adjoint-scaling` on a **different
host** from the generated results above, so do not compare their absolute
milliseconds against that section. Every column here is a ratio between two
quantities measured back to back on the same machine, which is what the
argument rests on; re-run `make bench` on the publishing host to fold them into
the generated block.

| family | parameters | nv | adjoint ratio | ratio ÷ parameters | ratio ÷ DOF |
|---|---|---|---|---|---|
| `weld_chain_1` | 10 | 1 | 6.5× | 0.650 | 6.5 |
| `weld_chain_4` | 40 | 1 | 7.9× | 0.198 | 7.9 |
| `weld_chain_16` | 160 | 1 | **9.1×** | **0.057** | 9.1 |
| `dof_chain_1` | 10 | 1 | 6.5× | 0.650 | 6.5 |
| `dof_chain_4` | 40 | 4 | 16.7× | 0.418 | 4.2 |
| `dof_chain_16` | 160 | 16 | **78.4×** | 0.490 | 4.9 |

Sixteen-fold more parameters at fixed DOF costs 40% more time. Sixteen-fold
more DOFs costs 12× more. The `ratio ÷ parameters` column collapses by an order
of magnitude down the first family and barely moves down the second —
**the adjoint's cost tracks DOF count and is very nearly independent of
parameter count**, which is what the implementation says it should be: the
parameter and contact channels ride two `O(nb)` sweeps, while the *state*
Jacobian costs `nq + nv` dual-ABA lanes per step.

That changes the comparison against finite differences, which the earlier
reading got backwards. At fixed DOF count, FD costs `2·n_params` rollouts and
the adjoint does not, so the adjoint's advantage grows with the parameter count
— measured, not projected:

| parameters (nv = 1) | adjoint | finite differences | adjoint speedup |
|---|---|---|---|
| 10 | 6.5× fwd | 19.8× fwd | **3.0×** |
| 20 | 7.6× fwd | 38.9× fwd | **5.1×** |
| 40 | 7.9× fwd | 79.9× fwd | **10.1×** |
| 80 | 7.8× fwd | 143.0× fwd | **18.4×** |

The gradient suite's near-unity `adjoint_speedup_vs_fd` was never evidence of a
bad asymptotic — it is what you get when a model has ten parameters per DOF and
only one or two DOFs, so the adjoint's constant factor has nothing to amortise
against. On a real system-identification problem, where parameters outnumber
DOFs, the adjoint wins by the margin above and keeps widening.

**What is still true, and what vector mode bought.** 6–9× the forward pass for
a single-DOF model is a large constant, and it is *not* the ~2–4× textbook
reverse mode delivers. The residual is the state Jacobian, and the first
reduction of it has landed: the `nq + nv` separately seeded dual passes through
ABA are now chunked into vector-mode passes of up to 16 tangent directions
each, so the primal — the kinematic tree walk, the joint transcendentals, the
memory traffic — is paid once per chunk instead of once per column.

Same suite, same host, adjoint ratio before and after:

| model | nv | scalar `Dual` | vector mode | speedup |
|---|---|---|---|---|
| `weld_chain_1` | 1 | 6.9× | 6.5× | 1.05× |
| `weld_chain_8` | 1 | 8.7× | 7.8× | 1.12× |
| `weld_chain_16` | 1 | 9.2× | 9.1× | 1.00× |
| `dof_chain_1` | 1 | 6.4× | 6.5× | 0.98× |
| `dof_chain_2` | 2 | 12.4× | 10.7× | 1.16× |
| `dof_chain_4` | 4 | 22.3× | 16.7× | 1.34× |
| `dof_chain_8` | 8 | 47.2× | 32.3× | **1.46×** |
| `dof_chain_16` | 16 | 97.6× | 78.4× | 1.24× |

**1.2–1.5× where it applies, not the 2–4× the idea promises**, and flat at one
DOF by construction: a single-coordinate block still takes the scalar path,
because at width 1 vector mode does identical arithmetic but cannot hoist its
dual-lifted inertias out of the time loop, which costs ~10% on a many-bodied
model. The win peaks at 8 DOF and falls away above it: the tangent arithmetic —
`N` multiply-adds per scalar operation — grows with the width, while the primal
it amortises does not. Vector mode removes the redundant primal; it cannot
remove the tangents, and on this workload the tangents are most of the cost.

Getting to a textbook constant needs a different lever — reverse mode within
the step, or an analytic `∂a/∂(q, v)` in the manner of the inertia channel —
not a wider forward mode. The parameter channel needs no work at all; it is
already asymptotically right.

Every gradient is **bit-identical** before and after — verified across chains
of 1–24 links plus a free-floating body with and without contact: the lane arithmetic is the same
expression in the same association order as the scalar path, asserted in
`multidual::tests::lanes_match_scalar_dual_bitwise` and
`adjoint::tests::vector_mode_matches_scalar_dual_bitwise`. Widening is a speed
change and never a numerical one, which is what lets it sit under a rollout
that promises bitwise reproducibility.

And exactness stands on its own: `adjoint_vs_fd_max_rel_err` ≈ 1e-10 with no
step size to choose and no truncation or cancellation error.

There is a scope note worth stating precisely, because this document used to
state it wrongly. The differentiable rollout does **not** require `nq == nv` or
single-DOF joints: `adjoint_rollout_gradient` handles spherical and free joints
(see `phyz_diff::rollout::adjoint`'s contract), and a free-floating body with
and without contact differentiates correctly today — it is one of the models
the vector-mode work above was verified against. What it requires is that `q0`
be laid out per `DofLayout`, which packs a free joint as
position-then-quaternion (`nq = 7`) where `Model` packs it as
exponential-coordinates-then-position (`nq = 6`). The gradient suite's scenes
are built as `Model`s, so feeding the ant to the adjoint needs that conversion
— a layout mismatch inside phyz, not a missing capability.

### Numerical quality

Energy error falls as O(dt) on both the simple and the chaotic pendulum over a
5 s horizon — exactly what a first-order symplectic integrator should do:

| dt | pendulum peak error | double pendulum peak error |
|---|---|---|
| 4×10⁻³ | 6.2×10⁻³ | 5.1×10⁻² |
| 2×10⁻³ | 3.1×10⁻³ | 2.6×10⁻² |
| 1×10⁻³ | 1.5×10⁻³ | 1.3×10⁻² |
| 2.5×10⁻⁴ | 3.9×10⁻⁴ | 3.3×10⁻³ |

Every 4× reduction in `dt` gives a 4× reduction in error, across a 16× range.
The integrator is correct.

Over a 100 s horizon the double pendulum's energy error stops responding to
`dt` entirely. That is not an integrator bug — it is chaos. By 100 s the
rollout has diverged onto a neighbouring trajectory and the energy band is set
by the horizon, not the timestep. The practical consequence: on a chaotic
system, buying accuracy with a smaller `dt` works on short horizons and stops
working on long ones. Both horizons are reported so the distinction is visible
rather than inferred.

---

## Reproducing and publishing

```bash
make bench          # -> bench-results.json, bench-results.md
make bench-gpu      # adds the GPU sweep
```

`bench-results.json` is the machine-readable artifact
(`schema_version: 1`, see [`report.rs`](crates/phyz-bench/src/report.rs)),
suitable for tracking over time and rendering on phyz.dev. Its `meta` block
carries hardware, OS, rustc version, library versions, and git commit — every
published number must travel with those.

### CI

The [`bench` workflow](.github/workflows/bench.yml) is **not** wired into the
main gate. It runs on manual dispatch and weekly, `continue-on-error`, purely
to prove the harness still runs and to produce an artifact. Timing numbers from
shared GitHub runners are noise, and a gate built on noise is one people learn
to ignore. Its output is stamped with a banner saying it is not a published
result.

Publishable numbers come from a known, idle machine, recorded by hand with
`make bench`.

### A note on the host

Benchmarking on a busy machine produces fiction. The spread column and the ⚠
flags exist so that fiction is visible when it happens. If you see ⚠ rows,
re-run on an idle host before believing them.
